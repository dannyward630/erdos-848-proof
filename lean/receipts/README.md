# Lean completion receipts

This directory intentionally contains no success receipt yet. A valid receipt
may be added only after `run_completion_gate.py` completes the clean source
build, trust-zero replay, live axiom capture, and post-build source/dependency
checks at the exact lock in `../source-lock.json`.

Static source inspection or `--preflight-only` output is not a receipt.
